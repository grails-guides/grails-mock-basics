include::{commondir}/common-runapp.adoc[]

NOTE: We have deleted the tests generated by the grails commands while creating domains and controllers. The only relevant test needed is `ClassroomServiceSpec`.

Run the tests from the `initial` folder:

[source, bash]
----
./gradlew test
----
